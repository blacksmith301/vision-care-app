import { VisionTest } from '@/components/vision-test/vision-test';

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-4 sm:p-8 lg:p-12">
      <VisionTest />
    </main>
  );
}
